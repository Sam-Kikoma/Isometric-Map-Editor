services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-isoedit}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: ${POSTGRES_DB:-isoedit}
    volumes:
      - pgdata:/var/lib/postgresql/data

  server:
    image: ${DOCKERHUB_USERNAME:-skikoma}/isometric-server:${IMAGE_TAG:-latest}
    expose:
      - "3001"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-isoedit}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-isoedit}
      JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET in .env}
      PORT: 3001
      FRONTEND_URL: ${FRONTEND_URL:?Set FRONTEND_URL in .env}
    depends_on:
      - db

  yjs-ws:
    image: ${DOCKERHUB_USERNAME:-skikoma}/isometric-yjs-ws:${IMAGE_TAG:-latest}
    expose:
      - "1234"

  client:
    image: ${DOCKERHUB_USERNAME:-skikoma}/isometric-client:${IMAGE_TAG:-latest}
    ports:
      - "80:80"
    depends_on:
      - server
      - yjs-ws

volumes:
  pgdata:
